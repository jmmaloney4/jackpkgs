# fixture-primary

Minimal dependency-free primary uv workspace used by
`tests/python-extra-workspaces.nix`. The lockfile is generated with
`uv lock` in this directory and should stay dependency-free so it never
goes stale.
