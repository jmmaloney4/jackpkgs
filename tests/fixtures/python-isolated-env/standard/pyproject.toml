[project]
name = "fixture-isolated-env"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["click>=8.1,<9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["README.md"]

[tool.uv]
index-url = "https://pypi.org/simple"
